/mvnw text eol=lf
*.cmd text eol=crlf
# Auto-detect text files and normalize
* text=auto

# Force shell scripts to LF
*.sh text eol=lf

# Force batch files to CRLF
*.bat text eol=crlf

# Optional: force specific file types (Java, XML, properties) to LF
*.java text eol=lf
*.xml text eol=lf
*.properties text eol=lf
